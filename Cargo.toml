cargo-features = ["codegen-backend"]
[package]
name = "midiplayer_rs"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.dev]
debug = 0
codegen-backend = "cranelift"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
